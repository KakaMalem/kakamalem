# Packages allowed to run install scripts are declared in package.json under
# "pnpm.onlyBuiltDependencies" (esbuild, sharp, unrs-resolver). Do not add
# dangerouslyAllowAllBuilds here: pnpm 12 rejects it and fails the install.
